---
phase: 12
plan: "02"
type: auto
wave: 1
depends_on: []
files_modified:
  - veritas/darknet/onion_detector.py
  - veritas/osint/ioc_detector.py
  - veritas/agents/scout.py
  - veritas/tests/unit/test_onion_detector.py
autonomous: true
requirements:
  - DARKNET-02
must_haves:
  truths:
    - "detect_onion_urls() extracts v2 (16-char) and v3 (56-char) .onion addresses"
    - "validate_onion() checks base32 format and checksum"
    - "classify_marketplace() returns marketplace/forum/exchange/unknown"
    - "IOC detector recognizes ONION_ADDRESS indicator type"
    - "Scout detects .onion URLs and routes via TOR (placeholder for 12-04)"
  artifacts:
    - path: "veritas/darknet/onion_detector.py"
      provides: ".onion URL validation and marketplace classification"
      min_lines: 80
    - path: "veritas/osint/ioc_detector.py"
      provides: "Extended with ONION_ADDRESS indicator type"
      min_lines: 5
    - path: "veritas/agents/scout.py"
      provides: ".onion URL detection and TOR routing hook"
      min_lines: 20
    - path: "veritas/tests/unit/test_onion_detector.py"
      provides: "Unit tests for onion detection and validation"
      min_lines: 60
  key_links:
    - from: "veritas/agents/scout.py"
      to: "veritas/darknet/onion_detector.py"
      via: "OnionDetector for _is_onion_url()"
    - from: "veritas/agents/scout.py"
      to: "veritas/darknet/tor_client.py"
      via: "TORClient for _capture_via_tor()"
    - from: "veritas/analysis/security/darknet.py"
      to: "veritas/darknet/onion_detector.py"
      via: "OnionDetector for .onion URL validation"
---

# Plan 12-02: .onion Validator and Marketplace Classifier

## Context

### Requirement Covered
- **DARKNET-02**: Hidden service detection algorithms (.onion validation, fingerprinting, marketplace structure detection)

### Current State (Pre-Plan)

**IOC Detection:**
- Basic `IOCDetector` exists in `veritas/osint/ioc_detector.py`
- Supports email, IP, URL, domain extraction
- No .onion URL validation
- No marketplace classification

**Scout:**
- `Scout` agent can navigate clearweb URLs
- No .onion URL routing support
- No hidden service detection

### Goal State (Post-Plan)

**Onion Detector:**
- `OnionDetector` class for .onion URL validation (v2 and v3 addresses)
- Base32 checksum validation
- Fingerprinting patterns for darknet sites
- Marketplace type classification (marketplace, forum, exchange, unknown)

**IOC Extension:**
- Extend `IOCDetector` to recognize .onion URLs as indicators
- Add darknet IOC types

**Scout Enhancement:**
- Add conditional routing for .onion URLs (placeholder for 12-04)
- Detect .onion URLs in page content

---

## Implementation Strategy

<task>
<files>
- veritas/darknet/onion_detector.py
- veritas/osint/ioc_detector.py
- veritas/agents/scout.py
- veritas/tests/unit/test_onion_detector.py
</files>
<action>
Create .onion URL validation and marketplace classification:

1. Create `veritas/darknet/onion_detector.py` with OnionDetector class:
   - PATTERN_V2 (16-char) and PATTERN_V3 (56-char) regex patterns
   - detect_onion_urls(text) - extracts and validates .onion URLs
   - validate_onion(onion_url) - checks base32 format and checksum
   - classify_marketplace(onion_url, page_content) - returns marketplace/forum/exchange/unknown
   - is_darknet_url(url) - quick .onion URL detection

2. Modify `veritas/osint/ioc_detector.py`:
   - Add ONION_ADDRESS to IndicatorType enum
   - Update extract_from_text() to call OnionDetector and create ONION_ADDRESS indicators

3. Modify `veritas/agents/scout.py`:
   - Add _is_onion_url(url) method using OnionDetector
   - Update capture_page() to detect .onion URLs and log warning (TOR routing placeholder for 12-04)

4. Create `veritas/tests/unit/test_onion_detector.py` with tests for:
   - validate_onion() with v2 and v3 addresses
   - detect_onion_urls() extracting multiple URLs
   - classify_marketplace() categorizing marketplace/forum/exchange
   - is_darknet_url() returning True for .onion, False for clearweb
</action>
<automated>
pytest veritas/tests/unit/test_onion_detector.py -v
</automated>
<done>
- Unit tests pass with pytest
- validate_onion() correctly validates v2 (16-char) addresses
- validate_onion() correctly validates v3 (56-char) addresses
- validate_onion() rejects invalid characters and lengths
- detect_onion_urls() extracts multiple .onion URLs from text
- classify_marketplace() correctly identifies marketplace, forum, exchange
- is_darknet_url() returns True for .onion URLs, False for clearweb
- IOC detector extracts .onion addresses with IndicatorType.ONION_ADDRESS
- Scout detects .onion URLs and logs warning (future TOR routing)
- Base32 checksum validation rejects malformed addresses
</done>
</task>

---

## Requirements Covered

| Requirement | Status | Notes |
|-------------|--------|-------|
| DARKNET-02 | 📝 Covered | .onion validation, fingerprinting, marketplace classification |

---

*Plan created: 2026-03-01*

