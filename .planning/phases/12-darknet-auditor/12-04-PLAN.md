---
phase: 12
plan: "04"
type: auto
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - veritas/analysis/security/darknet.py
  - veritas/agents/scout.py
  - veritas/agents/vision.py
  - veritas/agents/security_agent.py
  - veritas/config/darknet_rules.py
  - veritas/tests/unit/test_darknet_security_modules.py
autonomous: true
requirements:
  - DARKNET-04
must_haves:
  truths:
    - "DarknetAnalysisModule extends SecurityModule with MEDIUM-tier execution"
    - "Scout routes .onion URLs through TOR SOCKS5h proxy with graceful degradation"
    - "Marketplace security patterns detect escrow, crypto-only payments, Tor2Web"
    - "Privacy violation detection (clearnet bridges, third-party analytics)"
    - "SecurityAgent tier execution integrates DarknetAnalysisModule"
    - "CVSS scoring integrated for darknet findings"
  artifacts:
    - path: "veritas/analysis/security/darknet.py"
      provides: "DarknetAnalysisModule with TOR routing and marketplace patterns"
      min_lines: 200
    - path: "veritas/agents/scout.py"
      provides: "TOR proxy routing for .onion URLs via TORClient"
      min_lines: 50
    - path: "veritas/config/darknet_rules.py"
      provides: "Darknet severity rules and marketplace patterns (CVSS presets)"
      min_lines: 80
    - path: "veritas/agents/security_agent.py"
      provides: "DarknetAnalysisModule registration"
      min_lines: 10
    - path: "veritas/tests/unit/test_darknet_security_modules.py"
      provides: "Unit tests for DarknetAnalysisModule"
      min_lines: 100
  key_links:
    - from: "veritas/agents/scout.py"
      to: "veritas/darknet/tor_client.py"
      via: "TORClient async context manager in _capture_via_tor()"
    - from: "veritas/agents/scout.py"
      to: "veritas/darknet/onion_detector.py"
      via: "OnionDetector.is_darknet_url() in _is_onion_url()"
    - from: "veritas/analysis/security/darknet.py"
      to: "veritas/darknet/tor_client.py"
      via: "TORClient for .onion URL analysis"
    - from: "veritas/analysis/security/darknet.py"
      to: "veritas/darknet/onion_detector.py"
      via: "OnionDetector for .onion URL validation"
    - from: "veritas/agents/security_agent.py"
      to: "veritas/analysis/security/darknet.py"
      via: "register_security_module(DarknetAnalysisModule)"
    - from: "veritas/analysis/security/darknet.py"
      to: "veritas/config/darknet_rules.py"
      via: "get_darknet_severity() and get_cvss_preset()"
---

# Plan 12-04: VERITAS Darknet Agent Workflows

## Context

### Requirement Covered
- **DARKNET-04**: VERITAS darknet agent workflows (TOR-aware security modules, hidden service OSINT patterns, darknet vulnerability detection, privacy enforcement recommendations)

### Current State (Pre-Plan)

**Security Modules:**
- SecurityModule base class with tier execution exists (Phase 10)
- 25+ security modules for OWASP, PCI DSS, GDPR, headers, etc.
- No TOR-aware security modules
- No darknet-specific vulnerability detection

**Agents:**
- Scout has .onion detection hook (Plan 12-02)
- No TOR routing implementation
- Vision has no .onion content handling
- SecurityAgent has tier-based execution but no darknet integration

**Darknet Strategy:**
- `DarknetSuspiciousScoringStrategy` exists for Judge (darknet_suspicious.py)
- No corresponding security modules for deep analysis

### Goal State (Post-Plan)

**TOR-Aware Security Module:**
- `DarknetAnalysisModule` extending SecurityModule base class
- TOR client integration for .onion URL analysis
- MEDIUM-tier execution (15s timeout)
- Darknet-specific vulnerability patterns

**Scout TOR Routing:**
- Conditional routing for .onion URLs through TORClient
- Graceful degradation when TOR unavailable
- Privacy-preserving headers for .onion requests

**Vision .onion Handling:**
- Specialized handling for darknet UI patterns
- Tor2Web gateway detection in screenshots
- Marketplace interface recognition

**Darknet Security Rules:**
- Severity rules for darknet-specific findings
- CVSS scoring integration
- Privacy/anonymity enforcement recommendations

---

## Implementation Strategy

<task>
<files>
- veritas/analysis/security/darknet.py
- veritas/agents/scout.py
- veritas/config/darknet_rules.py
- veritas/agents/security_agent.py
- veritas/tests/unit/test_darknet_security_modules.py
</files>
<action>
Create TOR-aware darknet security module with agent integration:

1. Create `veritas/analysis/security/darknet.py` with DarknetAnalysisModule:
   - Extends SecurityModule with MEDIUM-tier, 15s timeout
   - Uses OnionDetector.is_darknet_url() to check .onion URLs
   - Uses TORClient to route .onion requests with graceful degradation
   - _analyze_marketplace_patterns() detects escrow, crypto-only payments
   - _analyze_privacy_violations() detects clearnet bridges, third-party analytics
   - _check_tor2web_exposure() detects Tor2Web gateway in Referer header
   - onion_address_only() helper for privacy

2. Modify `veritas/agents/scout.py` to implement TOR routing:
   - Add _capture_via_tor() method using TORClient async context manager
   - Update capture_page() to route .onion URLs through TOR
   - Return tor_routed=True flag in results
   - Graceful degradation: return 503 error on TOR unavailability

3. Create `veritas/config/darknet_rules.py` with:
   - DARKNET_SEVERITY_RULES dict mapping pattern_type to severity
   - DARKNET_CVSS_PRESETS with attack_vector, base_score for each pattern
   - TOR2WEB_GATEWAYS list for detection
   - MARKETPLACE_INDICATORS keyword patterns
   - get_darknet_severity() and get_cvss_preset() functions

4. Modify `veritas/agents/security_agent.py` to register module:
   - Import DarknetAnalysisModule
   - Call register_security_module(DarknetAnalysisModule)

5. Create `veritas/tests/unit/test_darknet_security_modules.py` with tests for:
   - DarknetAnalysisModule patterns detection
   - Scout TOR routing behavior
   - Darknet severity rules and CVSS presets
</action>
<automated>
pytest veritas/tests/unit/test_darknet_security_modules.py -v
</automated>
<done>
- Unit tests pass with pytest
- DarknetAnalysisModule correctly identifies .onion URLs
- _analyze_marketplace_patterns() detects escrow, listings
- _analyze_privacy_violations() detects clearnet bridges, analytics
- _check_tor2web_exposure() detects Tor2Web gateway headers
- Scout TOR routing routes .onion URLs through TORClient
- TOR unavailability returns graceful error finding
- Darknet severity rules are correct
- CVSS presets match severity levels
- DarknetAnalysisModule integrates with SecurityAgent tier execution
- OSCINT orchestrator queries darknet threat feeds
- Vision handles .onion content appropriately
- MEDIUM-tier execution respects 15s timeout
- TOR routing respects SOCKS5h proxy for DNS resolution
- No .onion URLs are exposed in logs
- onion_address_only() redacts full URLs
- All findings include "(read-only OSINT)" in recommendations
- No purchase/transaction capability exposed
</done>
</task>

---

## Requirements Covered

| Requirement | Status | Notes |
|-------------|--------|-------|
| DARKNET-04 | 📝 Covered | TOR-aware modules, hidden service OSINT, darknet vulnerability detection |

---

*Plan created: 2026-03-01*
*Security note: All findings include "(read-only OSINT)" - no purchase/transaction capability*

