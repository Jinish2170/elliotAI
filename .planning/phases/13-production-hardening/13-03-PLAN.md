---
phase: 13-production-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - veritas/darknet/onion_detector.py
  - veritas/core/tor_client.py
  - veritas/darknet/tor_client.py
  - veritas/tests/unit/test_onion_detector.py
  - veritas/tests/test_veritas.py
autonomous: true
requirements: [REPAIR-05, REPAIR-06]

must_haves:
  truths:
    - "OnionDetector regex patterns correctly match v2 and v3 .onion addresses"
    - "All 7 previously failing onion detector tests now pass"
    - "test_audit_tiers passes with 4 tiers"
    - "Only one TORClient implementation exists"
    - "Darknet module structure is clean with clear ownership"
  artifacts:
    - path: "veritas/darknet/onion_detector.py"
      provides: "Working onion URL detection"
    - path: "veritas/core/tor_client.py"
      provides: "Canonical TORClient implementation"
  key_links:
    - from: "veritas/darknet/onion_detector.py"
      to: "veritas/agents/scout.py"
      via: "IOCDetector import"
      pattern: "IOCDetector|OnionDetector"
---

<objective>
Fix broken tests and consolidate duplicate darknet modules.

Purpose: 8 tests are currently failing — 7 in onion_detector and 1 in test_audit_tiers. These must pass before any further work. Additionally, there are two TORClient implementations that need consolidation.

Output: All existing tests pass. Darknet modules have clear single ownership.
</objective>

<execution_context>
@C:/Users/hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-production-hardening/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OnionDetector regex and all 7 failing tests</name>
  <files>veritas/darknet/onion_detector.py, veritas/tests/unit/test_onion_detector.py</files>
  <action>
  1. Run the 7 failing tests to see exact error messages:
     `.venv/Scripts/python.exe -m pytest veritas/tests/unit/test_onion_detector.py -v --tb=short`

  2. Read `veritas/darknet/onion_detector.py` and find the regex patterns for v2 (.onion, 16 chars) and v3 (.onion, 56 chars).

  3. Fix the regex patterns:
     - v2 onion: `[a-z2-7]{16}\.onion`
     - v3 onion: `[a-z2-7]{56}\.onion`
     - Make sure the regex handles optional `http://` or `https://` prefix
     - Make sure it works with paths after the .onion domain

  4. If the tests have wrong expectations, fix the tests to match the onion address spec.

  5. Run all tests to confirm 0 failures.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest veritas/tests/unit/test_onion_detector.py -q --tb=short</automated>
  </verify>
  <done>All onion detector tests pass. v2 and v3 .onion address detection works correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_audit_tiers and consolidate duplicate TORClient</name>
  <files>veritas/tests/test_veritas.py, veritas/core/tor_client.py, veritas/darknet/tor_client.py</files>
  <action>
  1. In `veritas/tests/test_veritas.py`, find the `test_audit_tiers` test and change `assert len(settings.AUDIT_TIERS) == 3` to `assert len(settings.AUDIT_TIERS) == 4`. Also add assertions for darknet_investigation tier keys.

  2. Consolidate TORClient:
     - Read both `veritas/core/tor_client.py` and `veritas/darknet/tor_client.py`
     - Keep `veritas/core/tor_client.py` as the canonical implementation (it's from Phase 12)
     - Make `veritas/darknet/tor_client.py` re-export from core: `from veritas.core.tor_client import TORClient`
     - Grep for any imports of `veritas.darknet.tor_client` and ensure they still work

  3. Run full test suite to confirm everything passes.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest veritas/tests/test_veritas.py::TestSettings::test_audit_tiers veritas/tests/unit/ -q --tb=short</automated>
  </verify>
  <done>test_audit_tiers asserts 4 tiers. TORClient has single canonical implementation in core/ with re-export from darknet/.</done>
</task>

</tasks>

<verification>
- All onion detector tests pass (7 previously failing)
- test_audit_tiers passes (asserts 4 tiers)
- Full test suite: 0 failures
- Only one TORClient implementation
</verification>

<success_criteria>
All existing tests pass. No duplicate darknet implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/13-production-hardening/13-03-SUMMARY.md`
</output>
