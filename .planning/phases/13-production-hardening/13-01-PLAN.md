---
phase: 13-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - veritas/agents/scout.py
  - veritas/agents/scout/__init__.py
  - veritas/agents/scout_nav/__init__.py
autonomous: true
requirements: [REPAIR-01, REPAIR-02]

must_haves:
  truths:
    - "Scout captures real HTML page content via page.content()"
    - "Scout captures real HTTP response headers via response interception"
    - "Scout data is available for downstream agents (security, vision, graph)"
    - "Duplicate scout modules are consolidated"
  artifacts:
    - path: "veritas/agents/scout.py"
      provides: "Scout agent with proper HTML/header capture"
      contains: "page_content"
    - path: "veritas/agents/scout.py"
      provides: "ScoutResult dataclass with page_content and response_headers fields"
      contains: "response_headers"
  key_links:
    - from: "veritas/agents/scout.py"
      to: "veritas/core/orchestrator.py"
      via: "ScoutResult serialized to dict in scout_node"
      pattern: "page_content|response_headers"
---

<objective>
Fix Scout agent to capture real data that downstream agents need.

Purpose: The Scout is the "hands and legs" of the system — every downstream agent depends on what Scout captures. Currently, Scout doesn't capture actual page HTML or HTTP response headers, so SecurityAgent receives fabricated data. This makes security analysis unreliable.

Output: Scout that captures real page_content and response_headers, with duplicate scout modules consolidated.
</objective>

<execution_context>
@C:/Users/hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-production-hardening/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add page_content and response_headers to ScoutResult + capture them</name>
  <files>veritas/agents/scout.py</files>
  <action>
  1. Add two new fields to the ScoutResult dataclass:
     - `page_content: str = ""` — full HTML of the page
     - `response_headers: dict = field(default_factory=dict)` — HTTP response headers

  2. In the `investigate()` method, after page navigation completes:
     - Capture page HTML: `page_content = await page.content()`
     - Truncate to 500KB max to prevent memory bloat: `page_content = page_content[:512_000]`
     - Capture response headers via the navigation response object. Playwright's `page.goto()` returns a Response object. Use: `response = await page.goto(url, ...)` then `response_headers = dict(response.headers) if response else {}`

  3. In `navigate_subpage()`, do the same — capture page_content and response_headers.

  4. Store both in the returned ScoutResult.

  Note: Do NOT change the method signatures. The new fields have defaults so backward compatibility is preserved.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -c "from veritas.agents.scout import ScoutResult; r = ScoutResult.__dataclass_fields__; assert 'page_content' in r; assert 'response_headers' in r; print('OK: ScoutResult has page_content and response_headers')"</automated>
  </verify>
  <done>ScoutResult has page_content and response_headers fields. investigate() and navigate_subpage() capture real data from Playwright.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page_content and response_headers through orchestrator scout_node</name>
  <files>veritas/core/orchestrator.py</files>
  <action>
  In `scout_node()`, add two lines to the `result_dict` serialization:
  ```python
  "page_content": getattr(result, 'page_content', ''),
  "response_headers": getattr(result, 'response_headers', {}),
  ```

  In `security_node_with_agent()`, replace the fabricated page_content and headers with real data from scout results:
  ```python
  if scout_results:
      first_result = scout_results[0]
      page_content = first_result.get("page_content", "")
      headers = first_result.get("response_headers", {})
      # ... rest of dom_meta extraction stays the same
  ```

  Remove the fake HTML fabrication block and the hardcoded headers dict.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -c "
import ast, sys
with open('veritas/core/orchestrator.py') as f: src = f.read()
assert 'page_content' in src.split('result_dict')[1][:500], 'page_content not in result_dict'
# Check fabricated HTML is removed
assert 'f\"\"\"<html>' not in src, 'Fabricated HTML still present'
print('OK: orchestrator wires real page_content and response_headers')
"</automated>
  </verify>
  <done>Security node receives real HTML and headers from Scout instead of fabricated data.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate duplicate scout modules</name>
  <files>veritas/agents/scout/__init__.py, veritas/agents/scout_nav/__init__.py</files>
  <action>
  Determine which scout module set is canonical:
  - Read both `scout/scroll_orchestrator.py` and `scout_nav/scroll_orchestrator.py`
  - Read both `scout/lazy_load_detector.py` and `scout_nav/lazy_load_detector.py`
  - Check which one is imported by the main `scout.py`

  The canonical module is `scout_nav/` (has link_explorer.py which scout/ doesn't have).

  1. Ensure `veritas/agents/scout/__init__.py` re-exports from scout_nav if any imports point there
  2. If `scout/scroll_orchestrator.py` and `scout/lazy_load_detector.py` are unused duplicates, delete them
  3. Update any imports in `scout.py` that reference the wrong module

  Do NOT break existing imports — check with grep first.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -c "
from veritas.agents.scout import StealthScout, ScoutResult
print('OK: Scout imports work')
" 2>&1</automated>
  </verify>
  <done>Only one set of scroll_orchestrator/lazy_load_detector exists. No duplicate modules.</done>
</task>

</tasks>

<verification>
- ScoutResult dataclass has page_content and response_headers fields
- scout_node serializes both fields to the state dict
- security_node_with_agent uses real HTML and headers from Scout
- No fabricated HTML remains in the codebase
- Scout imports work correctly after module consolidation
</verification>

<success_criteria>
Scout captures real page HTML (up to 500KB) and HTTP response headers from Playwright, passes them through the orchestrator pipeline, and SecurityAgent receives this real data instead of fabricated HTML.
</success_criteria>

<output>
After completion, create `.planning/phases/13-production-hardening/13-01-SUMMARY.md`
</output>
