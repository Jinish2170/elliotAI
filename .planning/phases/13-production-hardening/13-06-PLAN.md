---
phase: 13-production-hardening
plan: 06
type: execute
wave: 3
depends_on: ["13-04", "13-05"]
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/lib/store.ts
  - frontend/src/hooks/useAuditStream.ts
  - frontend/src/components/landing/HeroSection.tsx
  - backend/services/audit_runner.py
autonomous: true
requirements: [REPAIR-11, REPAIR-12]

must_haves:
  truths:
    - "Frontend types match actual backend event schema"
    - "Zustand store handles all event types from audit_runner"
    - "Dead state fields are removed from the store"
    - "All 4 tiers are selectable from the landing page"
    - "Darknet tier has appropriate UI treatment (warning, distinct styling)"
    - "WebSocket stream recovers gracefully from node failures"
  artifacts:
    - path: "frontend/src/lib/store.ts"
      provides: "Clean Zustand store with actual event handling"
    - path: "frontend/src/lib/types.ts"
      provides: "Types matching backend reality"
  key_links:
    - from: "frontend/src/lib/store.ts"
      to: "backend/services/audit_runner.py"
      via: "WebSocket event schema"
      pattern: "handleEvent"
---

<objective>
Align frontend with actual backend data contracts and clean up dead code.

Purpose: The Zustand store has 80+ state fields, many never populated. Frontend types define structures the backend never sends. The landing page tier selector doesn't properly handle all 4 tiers. This plan makes the frontend honest about what data actually flows.

Output: Clean frontend store that matches what the backend actually sends.
</objective>

<execution_context>
@C:/Users/hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-production-hardening/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and clean Zustand store + types</name>
  <files>frontend/src/lib/store.ts, frontend/src/lib/types.ts</files>
  <action>
  1. Read `backend/services/audit_runner.py` `_handle_result()` and `_handle_progress()` methods to catalog every event type actually sent over WebSocket:
     - `phase_start`, `phase_complete`, `phase_error` — from _handle_progress
     - `audit_result` — from _handle_result (contains the full audit result)
     - `finding` — individual dark pattern findings
     - `security_module` — individual security module results
     - `screenshot` — screenshot data
     - `agent_personality` — agent theater events
     - `log_entry` — running log entries
     - `audit_complete` — final completion event
     - `audit_error` — error event

  2. In `store.ts`, trace the `handleEvent()` method and identify:
     - Events that are actually dispatched by the backend (KEEP)
     - State fields that are populated by handleEvent (KEEP)
     - State fields that are defined but never populated (MARK for cleanup)

  3. Do NOT delete type definitions from `types.ts` that components reference — instead mark unused fields with `// TODO: Wire from backend` comments so we know what's aspirational vs real.

  4. In the store, add a `// POPULATED` or `// NOT YET WIRED` comment next to each state field so future developers know what's real.

  5. Ensure `handleEvent` properly processes `audit_result` → extracts and stores the complete result with all sub-objects (trust_score, findings, recommendations, etc.)

  6. Add error handling in `handleEvent` — if an event type is unknown, log it but don't crash.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 || echo "TypeScript check complete (warnings OK, errors not OK)"</automated>
  </verify>
  <done>Store has clear annotations for what data is actually wired. handleEvent processes all backend event types. No runtime errors from unknown events.</done>
</task>

<task type="auto">
  <name>Task 2: Harden AuditRunner WebSocket streaming</name>
  <files>backend/services/audit_runner.py</files>
  <action>
  1. In `_handle_result()`, ensure all audit result sections are sent as separate typed events so the frontend can progressively update:
     - Send `finding` events for each dark pattern finding
     - Send `security_module` events for each security module result
     - Send `screenshot` events for each screenshot
     - Send the full `audit_result` event at the end

  2. Add timing information to events:
     ```python
     await send({
         "type": "phase_complete",
         "phase": phase,
         "elapsed_ms": int((time.time() - start_time) * 1000),
         ...
     })
     ```

  3. Ensure the `_handle_result` method extracts and sends:
     - `trust_score_result` — the final trust score with breakdown
     - `dual_verdict` — the dual-tier verdict (technical + plain English)
     - `security_summary` — aggregated security metrics
     These are currently in the raw result but may not be sent as events.
  </action>
  <verify>
    <automated>.\.venv\Scripts\python.exe -c "
from backend.services.audit_runner import AuditRunner
import inspect
src = inspect.getsource(AuditRunner._handle_result)
assert 'trust_score' in src.lower() or 'finding' in src, '_handle_result should send findings'
print('OK: AuditRunner sends typed events')
"</automated>
  </verify>
  <done>AuditRunner sends structured events for all result sections. Frontend can progressively render audit progress.</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles with no errors
- Store handleEvent processes all backend event types
- AuditRunner sends structured events for progressive rendering
- All 4 tiers work from the landing page
</verification>

<success_criteria>
Frontend accurately reflects what the backend sends. No dead state. Progressive rendering of audit results.
</success_criteria>

<output>
After completion, create `.planning/phases/13-production-hardening/13-06-SUMMARY.md`
</output>
